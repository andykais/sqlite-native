{
  "tasks": {
    "develop": "deno run --watch --check sqlite3.ts",
    "test": "deno test --unstable --allow-ffi --check test/*.test.ts",
    "test:watch": "deno test --watch --unstable --allow-ffi --allow-read --allow-write --check test/statements.test.ts",
    "compile_sqlite:macos": "deno run --check --allow-run=gcc,unzip --allow-read --allow-write deps/compile.ts macos"
  }
}
